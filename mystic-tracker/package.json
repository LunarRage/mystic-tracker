{
  "name": "mystic-tracker",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create": "graph create season4-Mystic --node https://dnaoversight.gg/deploy",
    "deploy": "graph deploy season4-Mystic --version-label v1.0.0 --node https://dnaoversight.gg/deploy",
    "create-local": "graph create --node http://localhost:8020/ mystic-tracker",
    "remove-local": "graph remove --node http://localhost:8020/ mystic-tracker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mystic-tracker",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.2",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
